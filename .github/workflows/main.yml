name: My Workflow

on: push

jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: My Step
        run: echo $MAPBOX_API_KEY
        env:
          MAPBOX_API_KEY: ${{ secrets.MAPBOX_API_KEY }}
